# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.pytest_cache/

# venv
venv/

# OS
.DS_Store

# Claude local state + guidance
.claude/
CLAUDE.md
AGENT.md
PROGRESS.md
DOCS_WRITING.md
REFINE.md
service/API_CONTRACT.md

# Large data — KHÔNG đẩy GitHub (raw blocked + train-data frozen giữ trên Drive)
data/
cleaned-data/
train-data/
artifacts/
legacy/

# Model output (checkpoint persist trên Drive, không vào repo)
retriever/checkpoints/
ranker/models/
runs/

# Map viz outputs (coords/reducer/html — sinh lại từ artifacts)
map/outputs/

# Secrets / API keys
.env

# Service cache (JSON fetch + dump recs)
service/backend/cache/

# Frontend (Node/Vite) — deps + build output, KHÔNG vào repo
service/frontend/node_modules/
service/frontend/dist/
service/frontend/.vite/
service/frontend/*.local
